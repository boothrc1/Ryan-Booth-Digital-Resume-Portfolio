import { RevealOnSrollDirective } from './reveal-on-sroll.directive';

describe('RevealOnSrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealOnSrollDirective();
    expect(directive).toBeTruthy();
  });
});
